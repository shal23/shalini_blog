<h3>Posts</h3>

<table class = "table table-striped">
  <tr>
    <th>Name</th>
    <th>Title</th>
    <th>Content</th>
    <th>Submitted on</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.title %></td>
    <td><%= truncate(post.content) %></td>
    <td><%= post.created_at.strftime("%e, %b, %Y") %></td>
    <!--<td><%= link_to 'Show', post %></td>-->
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Delete', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
<br>
<hr>
</br>
<!--<%= link_to 'Submit', new_post_path, :class => "btn btn-success", :style => "color:white;" %>-->
<br>
<br>
<!--<%= link_to 'Back to Posts', posts_path, :class => "btn btn-info", :style => "color:white;" %>-->